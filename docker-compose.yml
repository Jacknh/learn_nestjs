version: '3'
services:
  nestjs:
    image: 854db23ddf56
    # build: .
    ports:
      - 3000:3000
    environment:
      - RUNNING_ENV=dev
    volumes:
      - ./:/app
    command: npm run start:dev
